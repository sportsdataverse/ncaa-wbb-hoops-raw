# Auto detect text files and perform LF normalization
* text=auto

# Raw capture bundles are gzipped binary — never LF-normalize (would corrupt bytes)
wbb/raw/** binary
wbb/**/*.json.gz binary
*.json.gz binary

# Parquet is binary — `* text=auto` must never get a vote on it
*.parquet binary
